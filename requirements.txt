# Core (used in week 1 + 2, no network needed to run once installed)
numpy>=1.26
scikit-learn>=1.4

# Week 2: API backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Optional: real backends (see src/embeddings.py, src/llm.py)
# Uncomment if you want to switch away from the offline defaults.
# sentence-transformers>=2.6
# anthropic>=0.25

# Testing
pytest>=8.0
